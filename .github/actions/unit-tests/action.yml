name: Unit Tests
description: Unit Tests

runs:
  using: composite
  steps:
    - name: Run Unit Tests
      id: unit-tests
      shell: bash
      run: |
        echo "some unit tests"
        pwd
        ls
        cd gha-testing
        ls
